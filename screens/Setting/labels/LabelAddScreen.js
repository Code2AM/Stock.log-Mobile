import { NativeBaseProvider, Text } from "native-base"

const LabelAddScreen = () =>{

    return(
        <NativeBaseProvider>
            <Text>
                라벨 추가
            </Text>
        </NativeBaseProvider>
    )
}
export default LabelAddScreen;